import { extendTheme } from "@chakra-ui/react";

const extendedTheme = extendTheme({
  colors: {
    brand: {
      primary: "#1717DD",
      secondary: '#289BD6'
    },
  },
});

export { extendedTheme };
